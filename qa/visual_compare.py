from __future__ import annotations


def visual_compare_placeholder() -> dict:
    return {
        "status": "not-run",
        "reason": "Pixel diff/SSIM is intentionally left as a Phase 0 extension point.",
    }
